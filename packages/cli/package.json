{
  "name": "dsh-plugin-ops",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "DeepSeek Harness plugin operations CLI: scan, fix, gate 鈥?pre-boot health checks and failure attribution",
  "bin": {
    "dsh-ops": "lib/index.js"
  },
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "default": "./lib/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "lib"
  ],
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"require('fs').cpSync('assets','lib/assets',{recursive:true})\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"require('fs').rmSync('lib',{recursive:true,force:true})\""
  },
  "dependencies": {
    "dsh-plugin-ops-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
