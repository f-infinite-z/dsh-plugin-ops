{
  "name": "dsh-plugin-ops-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Workspace root for dsh-plugin-ops: published packages are dsh-plugin-ops-core and dsh-plugin-ops (bin: dsh-ops)",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
